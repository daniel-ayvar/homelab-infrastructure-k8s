apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: plex-secrets
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: infisical

  target:
    name: plex-secrets

  data:
    - secretKey: plex-claim-id
      remoteRef:
        key: MEDIA_SERVER__PLEX_CLAIM_ID
