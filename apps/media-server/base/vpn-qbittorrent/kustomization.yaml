apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - externalsecret.yaml
  - service.yaml
  - statefulset.yaml
  - pvc.yaml
labels:
- includeSelectors: true
  pairs:
    app.kubernetes.io/name: "vpn-qbittorrent"
